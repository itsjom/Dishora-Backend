<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme (dark) -->
    <!-- Base application theme (dark) -->
    <style name="Base.Theme.Dishora" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Navigation bar -->
        <item name="android:navigationBarColor">@color/white</item>

        <!-- Background -->
        <item name="android:windowBackground">@color/charcoal</item>

        <!-- AppBar background + text/icon colors -->
        <item name="colorPrimary">@color/dijon_mustard</item>
    </style>

    <style name="Theme.Dishora" parent="Base.Theme.Dishora" />
</resources>
